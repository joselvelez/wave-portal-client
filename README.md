#Simple dApp Skeleton using:

- React Framework
    - Global State using context to store theme, account, contract provider and signer

#To Do's
- Fix issue with theme not persisting in local storage. Currently refreshes on reset.